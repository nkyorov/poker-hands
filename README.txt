• Created a class simulating deck and various deck operations - deal, discard, shuffle.
• Implemented a class simulating poker hand and methods for adding and removing cards.
• Implemented a method, which returns details of the hand as a string containing two-character
representations of each card, separated by space and suits are rendered using the relevant Unicode symbols.
• User specifies a number of decks on the command line, each deck is then being shuffled and 10 cards are being dealt. Occurrences of different poker hands are then calculated.
